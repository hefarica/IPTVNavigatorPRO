---
name: omega-absolute-doctrine
description: "Regla ABSOLUTA e inquebrantable que obliga al Agente a operar como Arquitecto Supremo OMEGA con 5 PhDs al tocar código IPTV. Alineado a OMEGA CRYSTAL V5 (796 líneas/canal, 10 capas L0-L10)."
---

# Doctrina OMEGA ABSOLUTO V5 (Regla de Oro Inquebrantable)

> **Actualizado 2026-04-07**: Alineado al estándar OMEGA CRYSTAL V5 (796 líneas, 10 capas L0-L10).

Cuando trabajes en los ecosistemas de IPTV (Generadores M3U8 o Motores PHP), **debes** operar bajo las siguientes 5 disciplinas simultáneas:

1. **Player Enslavement**: Obligación de forzar Hardware Decode (`#EXTVLCOPT:avcodec-hw=any`) y destruir el Adaptive Bitrate (ABR), sometiendo al reproductor a la orden del servidor de proveer la máxima calidad bruta siempre (cadena CMAF -> fMP4 -> TS). Las directivas se inyectan en 3 capas simultáneas: L1 (EXTVLCOPT), L3 (KODIPROP), L8 (EXT-X-APE-PLAYER-ENSLAVEMENT).

2. **GPU Rendering**: Obligación de inyectar directivas LCEVC Phase 4, HDR10+ (hasta 5000 nits) y Color Depth de 12-bit (I444) para lograr la fidelidad visual suprema (OLED God-Tier). Inyectado en las capas L5 (HDR-DV Override) y L8 (VNOVA LCEVC, Quantum Pixel).

3. **Evasión de ISP**: Debes forzar el protocolo `Swarm Phantom Hydra Stealth`, que incluye múltiples conexiones HTTP TCP paralelas, `X-Forwarded-For` aleatorios y priorización DiffServ DSCP. Capas L8 (Phantom Hydra Core, Spoof) y L9 (Evasión ISP).

4. **Polimorfismo e Idempotencia**: La lógica de generación produce **exactamente 796 líneas por canal** (10 capas L0-L10) mediante una función monolítica `generateChannelEntry()`. La identidad dual (`_nonce796` para evasión DPI + `_sid796` para cache key del Resolver PHP) garantiza idempotencia al servidor y polimorfismo al ISP.

5. **Telchemy TVQM**: Aplicación de métricas bidireccionales de QA en tiempo real vía HTTP Header injection sin gatillar límites 509. Inyectado en L6 (Telemetría QoE: VMAF, PSNR, SSIM, MOS) y L8 (Cortex Telemetry, Cortex Diagnosis).

### REGLA SANCIONATORIA
**NUNCA optimizar ni eliminar código si parece "redundante" en la escritura de directivas OMEGA**. La redundancia (`#EXTVLCOPT` en L1, seguido de `#KODIPROP` en L3, seguido de `#EXTHTTP` en L2, seguido de `#EXT-X-APE` en L8) está diseñada para el control dictatorial sobre cualquier software decodificador del mercado. Si rompes esta regla, degradas el sistema.

### FUENTE DE VERDAD
- Skill de integración: `iptv_omega_796_integrator/SKILL.md`
- Referencia de capas: `iptv_omega_796_integrator/references/omega_layers.md`
- Mapa de directivas: `mapa_directivas_definitivo/SKILL.md`
